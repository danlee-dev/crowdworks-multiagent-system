# LangGraph Feature Flag Configuration
#
# Copy this file to .env and set USE_LANGGRAPH=true to enable LangGraph workflows
#
# 점진적 롤아웃 가이드:
# 1. 초기 테스트: USE_LANGGRAPH=false (기존 시스템 사용)
# 2. 개발 환경: USE_LANGGRAPH=true (LangGraph 테스트)
# 3. 프로덕션 10%: 로드 밸런서에서 10% 트래픽만 USE_LANGGRAPH=true 설정
# 4. 프로덕션 50%: 모니터링 후 50%로 증가
# 5. 프로덕션 100%: 안정성 확인 후 전체 전환

# LangGraph 활성화 여부 (true/false)
USE_LANGGRAPH=false

# LangSmith 트레이싱 (이미 .env에 설정되어 있어야 함)
# LANGSMITH_API_KEY=your_api_key_here
# LANGCHAIN_PROJECT=multiagent-rag-system

# 기타 설정은 기존 .env 파일 참조
